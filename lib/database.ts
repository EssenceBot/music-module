import type { ModuleDatabaseExports } from "@essence-discord-bot/types/types";
import * as schema from "../schema";

const database: ModuleDatabaseExports = {
  schema: schema,
};

export default database;
